/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GeneralResponseString } from '../../models/general-response-string';
import { MediaMessageRequest } from '../../models/media-message-request';

export interface CreateMessageWithFile$Params {
      body: MediaMessageRequest
}

export function createMessageWithFile(http: HttpClient, rootUrl: string, params: CreateMessageWithFile$Params, context?: HttpContext): Observable<StrictHttpResponse<GeneralResponseString>> {
  const rb = new RequestBuilder(rootUrl, createMessageWithFile.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<GeneralResponseString>;
    })
  );
}

createMessageWithFile.PATH = '/api/v1.0/message/files';
